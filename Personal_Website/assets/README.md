Place your résumé PDF here named `Andrew_Clark_Resume.pdf`.

This file is intentionally not included for privacy. To enable the embedded PDF viewer and download button in the Streamlit app, add your PDF to this folder with the exact filename above.

Example:

assets/
└── Andrew_Clark_Resume.pdf

Then run:

```bash
streamlit run app.py
```